// Test fib with tiny number to verify bigint works
func fib(n) {
    if (n <= 1) {
        return n;
    }
    return fib(n - 1) + fib(n - 2);
}

print("Testing fib with bigint support...");
var result = fib(20);
print("fib(20) = " + result);
print("Test completed!");
