// Test recursive fib with TCO (should be fast)
func fib_recursive(n) {
    if (n <= 1) return n;
    return fib_recursive(n - 1) + fib_recursive(n - 2);
}

print("Testing recursive fib with TCO...");
var result = fib_recursive(50);
print("fib_recursive(50) = " + result);
print("Test completed!");
