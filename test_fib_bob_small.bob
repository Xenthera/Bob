// Test Bob fib with smaller number for comparison
func fib_recursive(n) {
    if (n <= 1) return n;
    return fib_recursive(n - 1) + fib_recursive(n - 2);
}

print("Testing Bob recursive fib...");
var result = fib_recursive(35);
print("fib_recursive(35) = " + result);
print("Test completed!");
